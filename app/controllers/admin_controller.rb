class AdminController < ApplicationController
  # notes:
  # - allow admins to login/logout and change password (like in osCollect)
  # - CRUD access_token's (api_key's) linked to oscollect instance host_port's
end
